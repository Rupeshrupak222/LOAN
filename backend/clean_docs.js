const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const result = await prisma.document.deleteMany({ where: { status: 'PENDING', fileName: { contains: 'ChatGPT Image' } } }); console.log('Deleted ' + result.count + ' documents'); } main();
