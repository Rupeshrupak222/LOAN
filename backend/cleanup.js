const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { let retries = 5; while (retries > 0) { try { const docs = await prisma.document.findMany({ orderBy: { createdAt: 'desc' } }); if (docs.length > 8) { const toDelete = docs.slice(8).map(d => d.id); await prisma.document.deleteMany({ where: { id: { in: toDelete } } }); console.log('Deleted ' + toDelete.length + ' dummy documents, leaving exactly 8.'); } else { console.log('Only ' + docs.length + ' documents found. Nothing to delete.'); } return; } catch (err) { retries--; console.log('Retry...', err.message); await new Promise(r => setTimeout(r, 2000)); } } } main();
